- Fixed encoded patterns not appearing immediately in the output slot after normal encoding.
- Restored interface-terminal search string persistence and clear the interface-name filter when filling an input/output filter from an item.
- Keep filtered interface results visible after taking their last matching pattern until the search or filter changes.
- Added Shift + left-drag stack withdrawal across ME Storage entries, moving at most one stack per entry.
